import Image from "next/image";
import { FC, FunctionComponent, ReactNode } from "react";

type Props = {
  children: ReactNode;
};

const Home = () => {
  return <h3>Home</h3>;
};

export default Home;
